services:
  nginx:
    image: nginx:alpine
    container_name: static-sites
    restart: unless-stopped
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /var/www/static-sites:/var/www/static-sites:ro
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro
      # AllinSSL 部署的泛域名证书
      - /etc/ssl/certs/tmlab.site:/etc/ssl/certs/tmlab.site:ro
    healthcheck:
      test: ["CMD", "wget", "-q", "--spider", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
